import{ChangeDetectionStrategy,Component,input}from'@angular/core';let nextId=0;export type HgSessionStatTone='default'|'reward'|'success'|'danger'|'info';export interface HgSessionStat{label:string;value:string|number;tone?:HgSessionStatTone}
@Component({selector:'hg-session-result-card',template:`<section class="card" [attr.aria-labelledby]="headingId">@if(eyebrow()){<div class="eyebrow kr">{{eyebrow()}}</div>}@if(koreanTitle()){<div class="korean kr">{{koreanTitle()}}</div>}<h2 [id]="headingId">{{title()}}</h2>@if(subtitle()){<p>{{subtitle()}}</p>}<dl class="stats">@for(stat of stats();track stat.label){<div class="stat" [attr.data-tone]="stat.tone||'default'"><dd>{{stat.value}}</dd><dt>{{stat.label}}</dt></div>}</dl><div class="details"><ng-content select="[result-details]"/></div><div class="actions"><ng-content select="[result-actions]"/></div></section>`,styles:`.card{padding:var(--hg-space-7,2rem) var(--hg-space-6,1.5rem);border:1px solid var(--hg-border,#d8deea);border-radius:var(--hg-radius-block,1rem);background:var(--hg-card,#fff);text-align:center}.eyebrow,p,dt{color:var(--hg-text-muted,#647084)}.korean{margin:.25rem 0 .65rem;color:var(--hg-reward,#d79b00);font-size:clamp(2.5rem,8vw,3.5rem)}h2,p{margin:.45rem 0 0}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:.65rem;margin:1.5rem 0}.stat{display:grid;gap:.25rem;padding:.8rem .5rem;border-radius:.8rem;background:var(--hg-surface-muted,#f4f6fa)}dd,dt{margin:0}dd{font-size:1.35rem;font-weight:850}[data-tone=reward] dd{color:var(--hg-reward,#d79b00)}[data-tone=success] dd{color:var(--hg-jade,#15966d)}[data-tone=danger] dd{color:var(--hg-danger,#c43143)}[data-tone=info] dd{color:var(--hg-blue,#4f67ff)}.details:empty,.actions:empty{display:none}.actions{display:flex;justify-content:center;flex-wrap:wrap;gap:.75rem;margin-top:1.25rem}@media(max-width:880px){.stats{grid-template-columns:repeat(2,1fr)}.actions{flex-direction:column}}`,changeDetection:ChangeDetectionStrategy.OnPush})
export class HgSessionResultCardComponent{readonly eyebrow=input('');readonly koreanTitle=input('');readonly title=input.required<string>();readonly subtitle=input('');readonly stats=input.required<readonly HgSessionStat[]>();readonly headingId=`hg-session-result-${++nextId}`}
