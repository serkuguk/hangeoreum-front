import{ChangeDetectionStrategy,Component,booleanAttribute,input,output,signal}from'@angular/core';let nextId=0;
@Component({selector:'hg-file-picker',template:`<div class="picker"><input class="visually-hidden" [id]="id()" type="file" [accept]="accept()" [disabled]="disabled()" [attr.aria-label]="ariaLabel()||label()" (change)="selectFile($event)"/><label [for]="id()" [class.disabled]="disabled()"><span aria-hidden="true">📎</span><span>{{selectedName()||label()}}</span></label>@if(hint()){<small [id]="id()+'-hint'">{{hint()}}</small>}</div>`,styles:`.picker{display:grid;gap:.4rem}.visually-hidden{position:absolute;width:1px;height:1px;margin:-1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap}label{display:inline-flex;align-items:center;justify-content:center;gap:.55rem;min-height:2.75rem;border:1px dashed var(--hg-border,#ccd4e2);border-radius:.75rem;background:var(--hg-card,#fff);padding:.65rem 1rem;font-weight:700;cursor:pointer}input:focus-visible+label{outline:3px solid color-mix(in srgb,var(--hg-blue,#4f67ff) 28%,transparent)}label.disabled{cursor:not-allowed;opacity:.58}small{color:var(--hg-text-muted,#647084);font-size:.8rem}`,changeDetection:ChangeDetectionStrategy.OnPush})
export class HgFilePickerComponent{readonly id=input(`hg-file-${++nextId}`);readonly label=input('Выбрать файл');readonly ariaLabel=input<string|null>(null);readonly accept=input('');readonly hint=input('');readonly disabled=input(false,{transform:booleanAttribute});readonly fileSelected=output<File>();readonly selectedName=signal('');selectFile(event:Event):void{const el=event.target as HTMLInputElement;const file=el.files?.item(0);if(!file)return;this.selectedName.set(file.name);this.fileSelected.emit(file);el.value=''}}
