import{ChangeDetectionStrategy,Component,booleanAttribute,forwardRef,input}from'@angular/core';import{NG_VALUE_ACCESSOR}from'@angular/forms';import{HgControlValueAccessor}from'./control-value-accessor';let nextId=0;
@Component({selector:'hg-toggle',providers:[{provide:NG_VALUE_ACCESSOR,useExisting:forwardRef(()=>HgToggleComponent),multi:true}],template:`<label class="toggle" [for]="id()"><span class="copy"><span class="label">{{label()}}</span>@if(hint()){<small [id]="id()+'-hint'">{{hint()}}</small>}</span><input [id]="id()" [attr.name]="name()||null" type="checkbox" role="switch" [checked]="value()" [disabled]="disabled()||formDisabled()" [attr.aria-describedby]="hint()?id()+'-hint':null" (change)="update(($event.target).checked)" (blur)="markTouched()"/><span class="track" aria-hidden="true"><span class="thumb"></span></span></label>`,styles:`.toggle{display:flex;align-items:center;justify-content:space-between;gap:1rem;cursor:pointer}.copy{display:grid;gap:.2rem}.label{color:var(--hg-text,#172033);font-weight:700}small{color:var(--hg-text-muted,#647084);font-size:.8rem}input{position:absolute;width:1px;height:1px;opacity:0}.track{position:relative;flex:0 0 2.75rem;height:1.5rem;border-radius:999px;background:var(--hg-border,#bcc5d3)}.thumb{position:absolute;top:.2rem;left:.2rem;width:1.1rem;height:1.1rem;border-radius:50%;background:#fff;transition:transform .18s}input:checked+.track{background:var(--hg-blue,#4f67ff)}input:checked+.track .thumb{transform:translateX(1.25rem)}input:focus-visible+.track{outline:3px solid color-mix(in srgb,var(--hg-blue,#4f67ff) 28%,transparent);outline-offset:2px}`,changeDetection:ChangeDetectionStrategy.OnPush})
export class HgToggleComponent extends HgControlValueAccessor<boolean>{readonly id=input(`hg-toggle-${++nextId}`);readonly name=input('');readonly label=input.required<string>();readonly hint=input('');readonly disabled=input(false,{transform:booleanAttribute});constructor(){super(false)}update(value:boolean):void{this.updateValue(value)}}
